import React from 'react'

export const productForm = (props) => {
     const [Pid, setPid] = useState('')
     const [Pname, setPname] = useState('')
     const [status, setstatus] = useState('')
     const [PQuantity, setPid] = useState('')
  return (
    <div>

    </div>
  )
}
